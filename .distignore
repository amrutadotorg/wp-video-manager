# Production deployment allowlist for rsync.
# Keep only plugin PHP files and compiled frontend assets. The final rule excludes
# every other file or directory, including new development artifacts by default.
+ /dist/
+ /dist/*.js
+ /dist/*.css
+ /admin-users.js
+ /*.php
- *
